rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // CMS uploads: only authenticated users can write; anyone can read
    match /cms/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
